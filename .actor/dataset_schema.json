{
    "title": "SEC EDGAR Entity Resolver Dataset",
    "description": "Dataset items emitted by the actor.",
    "type": "object",
    "properties": {
        "type": {
            "type": "string",
            "enum": ["entity", "filing", "fact"]
        }
    }
}
