{
    "actorOutputSchemaVersion": 1,
    "title": "SEC EDGAR Entity Resolver Results",
    "description": "Links to dataset items for SEC EDGAR Entity Resolver.",
    "properties": {
        "results": {
            "type": "string",
            "title": "Results",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        }
    }
}
